<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Authors</title>
</head>

<body>

<h1>List authors </h1>



<table border="1">
    <thead>
        <tr>
            
            <th>Username </th>
           
             <th>email</th>
           
  <th>Action</th> <!-- Ajoutez une colonne pour les actions -->

        </tr>
    </thead>
    <tbody>
        {% for t in response %}
            <tr>
            
               
               
                <td>{{ t.Username }}</td>
              
                <td>{{ t.email}}</td>
                 <td>
                    <a href="{{ path('edit_auther', {'id': t.id}) }}">edit</a> |
                    <a href="{{ path('rm', {'id': t.id}) }}">delete</a>
                </td>
            </tr>
        {% endfor %}
       <tr>
       <h3> <a href="{{ path('add') }}">Ajouter autre</a></h3>
        </tr>
    </tbody>
</table>
